# BearCode 项目梳理 —— 面试准备

---

## 一、项目一句话介绍

**BearCode 是一个基于 Python 的自进化 Harness Agent Runtime**，统一编排大模型推理、工具调用、权限控制、Memory、Skills、MCP、子 Agent 和上下文压缩，并从用户反馈中持续沉淀可复用能力。

---

## 二、项目定位

| 维度 | 说明 |
|------|------|
| **不是** | 一个简单的 CLI 聊天工具、一个模型 API 的壳 |
| **而是** | 一个可运行、可阅读、可扩展的本地 Coding Agent Runtime |
| **核心差异** | Skills 自进化 + 在线评测：把用户反馈转化为可复用、可审计、可演化的长期能力 |

---

## 三、技术栈

```text
Python 3.11+, AsyncIO, OpenAI SDK, Anthropic SDK
MCP (Model Context Protocol), JSON-RPC (自研实现)
Docker, Rich (终端 UI), PyPDF2, pdfplumber
ripgrep (代码搜索)
```

**关键词**：Harness Agent, Self-Evolving Agent, Skills Evolution, Tool-Using Agent, MCP Client, Multi-Agent, Long-term Memory

---

## 四、架构设计（六层结构）

```
入口层   agents/main.py, agents/ui.py        → CLI/REPL/参数解析
Harness层 agents/agent.py                     → Agent Loop/模型协议/工具调度
能力层   tools.py, skills.py, memory.py,      → 文件/Shell/Skills/Memory/MCP/子Agent
         mcp_client.py, subagent.py
Prompt层 agents/prompt.py                     → 动态构建系统提示词
演化层   online_skill_evolution.py,           → 自进化抽取/决策/落盘
         skill_evolution.py
持久化层 .bear/, ~/.bear/, ~/.BearCode/       → Skills/Memory/Session/审计
```

### 核心运行链路

```text
用户输入
  → main.py (CLI/REPL)
  → Agent.chat()
  → 初始化 MCP / 检索 Skills / 预取 Memory
  → 根据协议选择 _chat_openai() 或 _chat_anthropic()
  → 模型返回文本或 tool_call
  → Harness 做权限检查
  → execute_tool() 执行工具 / Skill / MCP / 子 Agent
  → tool_result 回写模型 → 循环
  → 保存 Session / Skill usage tracking / Online Skill Evolution
```

---

## 五、目录结构

```text
BearCode/
├── agents/
│   ├── main.py                    # CLI 入口、REPL、参数解析
│   ├── agent.py                   # Agent Runtime、模型调用、工具调度、上下文压缩
│   ├── tools.py                   # 内置工具和权限系统
│   ├── prompt.py                  # System prompt 动态构建
│   ├── skills.py                  # Skills 加载、检索、执行、创建和演化封装
│   ├── online_skill_evolution.py  # 在线 Skill 抽取和 add/merge/discard 决策
│   ├── skill_evolution.py         # Skill 落盘、版本快照、审计统计
│   ├── online_skill_eval.py       # 在线 Skills 评测（replay+LLM judge+champion）
│   ├── memory.py                  # 长期记忆系统
│   ├── mcp_client.py              # MCP stdio JSON-RPC 客户端（自研）
│   ├── subagent.py                # 子 Agent 配置
│   ├── session.py                 # 会话保存与恢复
│   ├── session_memory.py          # 会话级结构化折叠记忆
│   └── ui.py                      # 终端 UI 输出
├── .bear/
│   ├── skills/                    # 项目级 Skills
│   └── skill-evolution/           # 自进化审计产物
├── wiki/                          # 项目文档中心
├── Dockerfile
├── requirements.txt
└── README.md
```

---

## 六、核心功能模块

### 6.1 Agentic Harness（运行时编排）

- 模型只负责推理和提出工具调用意图，**真正操作环境由 Harness 统一管理**
- 支持 OpenAI-compatible 和 Anthropic-compatible 两类模型协议
- 完整的 Agent Loop：推理 → 工具调用 → 权限检查 → 执行 → 结果回写 → 继续推理

### 6.2 工具系统与权限控制

**内置工具**：read_file, write_file, edit_file, list_files, grep_search, run_shell, skill, agent 等

**权限层次**：
| 模式 | 说明 |
|------|------|
| default | 危险操作需用户确认 |
| acceptEdits | 自动允许编辑，推荐用于自动沉淀 Skills |
| Plan Mode | 只读探索，阻断写操作和 Shell |
| bypassPermissions | 跳过确认（YOLO） |
| dontAsk | 自动拒绝需确认操作，适合 CI |

**安全机制**：
- 编辑文件前必须先 read_file，并记录 mtime 校验
- 危险 Shell 命令需要确认
- Plan Mode 下禁止写操作

### 6.3 Skills 体系

**Skill** 是一个可复用能力说明文件（`SKILL.md`），保存的是方法/流程/规范，不是一次性内容。

- **存储**：项目级 `.bear/skills/` 或用户级 `~/.bear/skills/`
- **检索**：BM25-lite 轻量词面检索，根据用户请求动态注入
- **执行**：inline 模式（返回 Skill prompt）或 fork 模式（独立子 Agent 执行）
- **管理**：支持手动创建、演化、反馈统计

### 6.4 Skills 自进化（核心亮点）

这是项目最核心的差异化能力：

```text
第 N 轮：Agent 完成任务 → 保存 pending extraction window
第 N+1 轮：用户反馈到来 → 合并为证据
  → Extractor：从对话窗口抽取可复用候选 Skill（最多一个）
  → Maintainer：结合已有 Skills 做相似检索
     → 判断 add（新增）/ merge（合并）/ discard（丢弃）
  → 调用 create_skill_file() 或 evolve_skill_file()
  → 写入 SKILL.md
  → 记录 provenance、usage stats、版本快照
```

**三层防垃圾机制**：
1. Extractor 规则过滤：不沉淀一次性内容、隐私、URL、临时项目事实
2. Maintainer 相似检索：优先 merge 或 discard，避免重复膨胀
3. Usage tracking：长期不用可归档

### 6.5 在线 Skills 评测

一条完整的质量观察链路：

```text
/skill-eval
  → 读取 online provenance / usage stats / active Skills
  → 按 Skill 形成 lineage
  → 从历史对话构造 replay pool
  → 从 SKILL.md 编译规则：
     - programmatic 规则（非空、JSON、表格、引用来源等）
     - LLM judge 规则（skill_instruction_alignment）
  → 评估历史 latest_assistant 是否满足规则
  → 基于失败规则生成 candidate variants（heuristic + LLM）
  → 用候选变体重新生成 replay 回复并评测
  → 结合 retrieved / relevant / used 判断状态：
     unobserved → incubating → watch → healthy
  → champion promotion：样本/规则/usage gate 达标时记录健康版本
  → 默认不自动覆盖 active SKILL.md
```

### 6.6 Memory 长期记忆

- Memory 记**事实和偏好**（用户喜欢先看结论、项目当前目标）
- Skills 记**方法和流程**（代码审查流程、报告写作规范）
- 文件型存储，按项目路径 hash 隔离
- side query 选择相关记忆注入当前对话

### 6.7 MCP 外部工具接入

- **自研** stdio JSON-RPC MCP Client（不依赖 MCP SDK）
- 支持读取 `.mcp.json` / settings 配置
- 启动 MCP Server → initialize → tools/list → 包装为 `mcp__server__tool`
- 工具调用时路由到对应 server 的 tools/call

### 6.8 子 Agent

- 类型：explore（只读探索）、plan（只读方案）、general（全功能）
- 支持自定义子 Agent（项目级 + 用户级）
- 隔离上下文，降低主 Agent 消息历史膨胀
- Skill 的 fork 模式也复用子 Agent 机制

### 6.9 上下文压缩与会话恢复

- **多级压缩**：消息截断、大工具结果持久化、compact_context 折叠
- **会话级折叠记忆**（session_memory.py）：结构化 JSON 记忆（episode/working/tool），最大化保留任务信息
- **会话保存**：自动保存 + `--resume` 恢复
- **预算控制**：token 预算、费用上限、最大轮次

---

## 七、面试讲法（三种时长）

### 30 秒版

BearCode 是我用 Python 实现的一个自进化命令行 Coding Agent。核心是自研 Agentic Harness，统一编排模型推理、工具调用、权限、Memory、Skills、MCP 和子 Agent。最大亮点是 Skills 自进化与评测：把用户反馈沉淀成 `SKILL.md`，再用 replay、LLM judge、candidate variants 和 champion 机制评估 Skill 质量。

### 1 分钟版

这个项目主要解决两个问题：第一，怎么把大模型变成能安全执行软件工程任务的本地 Agent；第二，怎么让 Agent 在使用过程中沉淀能力。

我实现了一个 Python Agentic Harness，负责模型调用、tool call 解析、权限检查、工具执行、结果回写、上下文压缩和会话保存。上层支持文件读写、代码搜索、Shell、Plan Mode、双协议适配、Memory、Skills、MCP 和子 Agent。

最核心的亮点是自进化 Skills 和在线评测。系统会用下一轮用户反馈作为证据，由 Extractor 抽取可复用候选，Maintainer 判断新增/合并/丢弃。评测阶段用真实 replay 样本、规则编译、LLM judge 和 candidate variants 做质量评估。整个过程有权限控制、provenance 记录和版本快照。

### 2 分钟深度版

BearCode 的底层是一个自研 Agentic Harness。用户输入进入 `Agent.chat()` 后，系统初始化 MCP、检索 Skills、预取 Memory，然后根据配置选择 OpenAI 或 Anthropic 协议。模型返回 tool call 后，Harness 先走统一权限检查，再分发到内置工具、Skill、MCP 工具或子 Agent，执行结果回写给模型，形成推理-行动-观察-继续推理的闭环。

这个项目做了几个重要扩展：
- **Plan Mode**：大改动前只读分析和写计划，审批后再执行
- **Memory 和 Skills 分层**：Memory 记事实，Skills 记方法
- **自研 MCP Client**：stdio JSON-RPC 动态接入外部工具
- **子 Agent**：隔离上下文处理探索、规划和局部任务

最重要的是 **Skills 自进化和在线评测**。系统把对话窗口和下一轮反馈组成证据，用 Extractor 抽取可复用规则，Maintainer 做相似检索和决策。`/skill-eval` 基于在线日志构造 replay 池，从 Skill 文本编译规则，使用程序检查和 LLM judge 评估历史回复，失败时生成 candidate variants 重新试跑。只有证据足够且候选优于当前版本时，才记录 champion 产物，保证自进化可控、可追溯、可比较。

---

## 八、STAR 表达

### S（背景）
普通大模型工具完成任务后，经验只停留在当前上下文；用户下次提出同类任务仍需重复说明偏好和流程。同时，让模型直接操作文件和 Shell 存在安全风险。

### T（目标）
实现一个本地 Harness Agent，不仅能安全执行软件工程任务，还能从用户反馈中沉淀可复用能力，形成项目级和用户级 Skills。

### A（行动）
1. 设计 Agentic Harness，统一处理模型请求、工具调用、权限检查、结果回写和会话保存
2. 实现文件读写、精确编辑、代码搜索、Shell、Plan Mode、MCP、子 Agent 和 Skills 调用
3. 设计 Skills 自进化链路：pending window → Extractor 抽取 → Maintainer 决策 → SKILL.md 落盘
4. 实现在线 Skills 评测：replay pool → 规则编译 → LLM judge → candidate variants → champion registry
5. 自行实现 MCP Client（stdio JSON-RPC），支持外部工具动态发现和调用路由
6. 设计 Memory + Skills 分层，避免项目事实误沉淀为任务方法
7. 实现上下文压缩、会话恢复、指数退避重试等工程可靠性机制

### R（结果）
形成了可运行的自进化 Harness Agent：既能完成代码阅读、文件编辑、测试执行等 Coding Agent 任务，又能将稳定用户反馈沉淀为可复用 Skills，并通过 replay 评测、LLM judge、candidate 试跑和 champion 产物回溯来源、版本、质量和使用效果。

---

## 九、高频面试问题

### Q1：为什么叫 Harness Agent？

普通聊天机器人是"用户输入→模型回复"。BearCode 的核心是 Harness：模型只提出 tool call，真正的文件读写、Shell、Skill、MCP 和子 Agent 都由 Runtime 统一做权限检查、执行调度、结果回写和状态保存。它有完整的**推理→行动→观察→继续推理**闭环。

### Q2：Skills 自进化是怎么实现的？

每轮回复后保存 pending window。下一轮用户输入作为反馈证据合并进去，进入 `online_ingest()`：Extractor 抽取最多一个可复用候选 Skill，Maintainer 结合已有 Skills 做相似检索，判断 add/merge/discard，最后写入 `SKILL.md`，记录 provenance 和版本快照。

### Q3：怎么避免沉淀垃圾 Skill？

三层控制：
1. **Extractor 规则**：不沉淀一次性内容、隐私、URL、精确日期、临时项目事实
2. **Maintainer 相似检索**：优先 merge 或 discard，避免重复膨胀
3. **Usage tracking**：长期不用可归档

评测层还有保护：Skill 不会因创建就默认健康，必须有 replay 样本和规则通过率支撑。Candidate variants 只写评测产物，不自动覆盖 active Skill。

### Q4：Memory 和 Skills 的区别？

- **Memory**：记事实和偏好（用户喜欢先看结论、项目当前目标）——解决"知道什么"
- **Skills**：记方法和流程（代码审查流程、报告写作规范）——解决"怎么做"

### Q5：怎么保证 Agent 不乱改文件？

1. **Plan Mode**：大改动前只读探索并写计划
2. **统一权限检查** `check_permission()`：危险 Shell、新建文件、Skill 创建会确认
3. **编辑前读取 + mtime 校验**：防止基于过期文件内容覆盖用户修改

### Q6：MCP 是怎么接入的？

自研 stdio JSON-RPC MCP Client（不依赖 MCP SDK）。读取 `.mcp.json` 或 settings 配置，启动 MCP Server 子进程，执行 initialize 和 tools/list，把外部工具包装为 `mcp__server__tool`，模型调用时路由到对应 server 的 tools/call。

### Q7：为什么要做子 Agent？

用来隔离上下文和任务范围。explore 只读搜索代码，plan 只读输出方案，general 处理局部任务。主 Agent 不需要承载所有搜索细节，复杂任务也能拆分成更小的独立上下文。Skill 的 fork 模式也复用这个机制。

### Q8：项目最大的技术难点是什么？

难点不是调用模型 API，而是把**模型、工具、权限、上下文、长期记忆和自进化沉淀串成稳定闭环**。尤其是自进化部分，既要能自动沉淀用户反馈，又不能让模型无约束修改自己的能力，所以需要 pending window、Extractor、Maintainer、权限控制、版本快照和 provenance 审计。

### Q9：上下文压缩是怎么做的？

多级压缩策略：
1. **工具结果截断**：超过 60% 上下文的 read_file/run_shell 结果会被截断
2. **大结果持久化**：超长结果写入文件，只留引用在上下文中
3. **compact_context**：触发模型将历史对话折叠为结构化 session memory JSON（episode/working/tool 三类记忆）
4. **指数退避重试**：429/503 等可重试错误自动退避重试

### Q10：双协议适配有什么价值？

不是"多写一个 if"。真正价值在于：**把协议差异限制在 Agent 模型调用层**，工具、权限、Skills、Memory、MCP 不需要关心底层是 OpenAI 还是 Anthropic。这样项目可以接入不同供应商或代理网关。

---

## 十、简历 Bullet 精选

### Harness 与架构
- 自研 Python Agentic Harness，统一编排模型推理、工具调用、权限检查、结果回写、上下文压缩和会话恢复
- 设计 OpenAI/Anthropic 双协议适配层，复用统一工具定义并适配不同后端的 tool call 消息格式
- 构建可恢复长会话机制，支持自动保存 session、`--resume` 恢复、工具结果截断和上下文 compact

### 工具与权限
- 实现文件读取、精确编辑、代码搜索、Shell、Skill、MCP、子 Agent 等工具系统，通过统一权限层控制执行边界
- 设计 Plan Mode 只读规划流程，复杂修改前阻断写操作和 Shell
- 实现编辑前读取和 mtime 校验，避免基于过期文件内容覆盖用户修改

### Skills 自进化与评测
- 设计在线 Skills 自进化链路，将用户后续反馈抽取为可复用任务规则，自动创建或合并 SKILL.md
- 构建在线 Skills 评测系统，从 provenance 构造 replay pool，编译 programmatic/LLM judge 规则，输出 rule pass 和状态门控结果
- 设计 candidate variant 试跑机制，基于失败规则生成 heuristic/LLM 改进候选，重新生成 replay 回复并复用同一套规则评测
- 实现 champion promotion 记录机制，样本/规则/usage gate 达标时才写入 champion 产物，默认不覆盖 active Skill

### Memory / MCP / 子 Agent
- 实现文件型长期 Memory，按项目路径 hash 隔离，通过 side query 选择相关记忆注入对话
- 自研 stdio JSON-RPC MCP Client，支持 MCP Server 发现、工具包装和调用路由
- 支持 explore/plan/general 子 Agent，用隔离上下文处理探索、规划和局部任务

---

## 十一、项目关键数据

| 维度 | 数据 |
|------|------|
| 核心源码 | ~10+ 个 Python 模块，数千行 |
| 源文件 | agents/ 下 16 个文件 |
| 支持的模型协议 | OpenAI-compatible + Anthropic-compatible |
| 内置工具数量 | 15+ |
| Skills 状态 | 4 种（unobserved/incubating/watch/healthy） |
| 权限模式 | 5 种 |
| MCP 实现 | 自研 stdio JSON-RPC Client |
| 子 Agent 类型 | explore / plan / general + 自定义 |
| 架构分层 | 6 层（入口/Harness/能力/Prompt/演化/持久化） |

---

## 十二、一句话总结

> BearCode 是一个**足够轻、足够清楚、足够可改、足够可扩展**的 Agent Runtime，验证当模型能力到位后，系统如何把用户反馈持续转化为**可复用、可审计、可演化**的长期能力。

---

## 十三、给面试官的项目描述模板

Bear Agent 是一个基于 Python 的自进化命令行 Coding Agent Runtime。项目通过自研 Agentic Harness 统一编排 OpenAI/Anthropic 兼容模型、工具调用、权限控制、Memory、Skills、MCP 和子 Agent；同时实现在线 Skills 自进化与评测闭环，将用户稳定反馈沉淀为 SKILL.md，并通过 replay pool、规则编译、LLM judge、candidate variants、usage gate 和 champion registry 对 Skill 质量进行可审计评估。
