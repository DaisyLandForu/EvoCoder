# Bear Code 面经：模拟面试问答

本文基于当前代码、`README.md` 和 `wiki/` 文档整理，用于围绕 Bear Code 项目进行面试演练。建议回答主线固定为：

```text
自研 Agentic Harness
  -> 统一编排模型、工具、权限、上下文和持久化
  -> 支持 Skills / Memory / MCP / 子 Agent
  -> 从用户反馈中自动沉淀和演化 Skills
  -> 用在线回放 / 规则 / 模型辅助判断 / 候选版本试跑 / 健康版本记录评测 Skill 质量
  -> 形成可执行、可沉淀、可审计、可扩展的本地 Coding Agent Runtime
```

## 一、开场与项目定位

### Q1：请你先介绍一下 Bear Code 这个项目。

**回答：**  
Bear Code 是我用 Python 实现的一个本地命令行 Coding Agent Runtime。它不是简单套一层大模型 API，而是实现了一套 Agentic Harness：由 Runtime 统一编排模型推理、工具调用解析、权限检查、工具执行、结果回写、上下文压缩、会话恢复、Memory、Skills、MCP 外部工具和子 Agent。

它的核心设计是把“模型推理”和“环境动作”分开。模型只负责分析任务并提出工具调用意图，比如读文件、编辑文件、执行 Shell、调用 Skill 或 MCP 工具；真正触碰文件系统和本地环境的动作由 Harness 做权限判断、执行和审计。

项目最有辨识度的部分是 Skills 自进化与评测。系统会把用户的稳定反馈沉淀成 `SKILL.md`，并记录来源、版本快照和使用效果，再通过在线回放、规则评测、模型辅助判断、候选版本试跑和健康版本记录来治理这些 Skills。

### Q2：一句话怎么概括这个项目？

**回答：**  
Bear Code 是一个自研的自进化 Harness Agent，它把大模型工具调用、权限控制、长期上下文、外部工具扩展和 Skills 自进化评测组织成一个可运行、可追踪、可扩展的本地 Coding Agent Runtime。

### Q3：这个项目和普通 ChatGPT CLI 有什么区别？

**回答：**  
普通 ChatGPT CLI 基本是“用户输入 -> 调模型 -> 打印文本”。Bear Code 的核心是 Harness：模型可以发起工具调用，但不直接操作本地环境。所有工具调用都会经过 Runtime 的统一路由和权限判断，然后执行结果再回写给模型，形成“推理 -> 行动 -> 观察 -> 继续推理”的闭环。

此外，Bear Code 还有长期能力沉淀。普通 CLI 的经验只停留在当前上下文里，窗口清掉就没了；Bear Code 会把用户稳定反馈抽取成 Skills，并通过后续检索、调用和在线评测持续验证这些 Skills 是否真的有用。

### Q4：项目规模和技术栈是什么？

**回答：**  
核心源码在 `agents/` 目录，目前核心 Python 代码大约 7,800 多行；如果算上 `.bear/skills` 里的脚本，全仓库 Python 代码大约 9,100 多行。

技术栈主要是 Python、AsyncIO、OpenAI SDK、Anthropic SDK、stdio JSON-RPC、MCP、Docker，以及 Markdown 形式的 Skills / Memory / 文档体系。项目不依赖重型 Agent 框架，主要目的是从底层理解和实现 Claude Code 类 Coding Agent 的关键机制。

## 二、整体架构

### Q5：项目整体架构怎么分层？

**回答：**  
我把项目分为六层：

| 层级 | 主要文件 | 职责 |
|------|----------|------|
| 入口层 | `agents/main.py`、`agents/ui.py` | CLI 参数解析、REPL、一次性任务、用户确认、终端输出 |
| Harness 层 | `agents/agent.py` | Agent Loop、模型协议适配、工具调度、上下文压缩、会话保存、后台任务 |
| 能力层 | `agents/tools.py`、`agents/skills.py`、`agents/memory.py`、`agents/mcp_client.py`、`agents/subagent.py` | 内置工具、Skills、Memory、MCP、子 Agent |
| Prompt 层 | `agents/prompt.py` | 动态拼接系统提示词、项目规则、Skills、Memory、子 Agent、deferred tools |
| 演化层 | `agents/online_skill_evolution.py`、`agents/skill_evolution.py`、`agents/online_skill_eval.py` | Skill 抽取、落盘、审计、评测和健康版本记录 |
| 持久化层 | `.bear/`、`~/.bear/`、`~/.BearCode/`、`~/.bear-code/` | Skills、Memory、Session、审计记录、大工具结果、Plan 文件 |

这个分层的好处是：`agent.py` 是调度中心，但具体能力拆在独立模块里，模型协议、工具执行、长期记忆和自进化逻辑不会互相纠缠。

### Q6：一次用户请求在系统内部怎么流转？

**回答：**  
用户输入首先进入 `agents/main.py`，REPL 或 one-shot 模式会进入主对话流程。在这个流程里，系统会：

1. 首次对话时懒加载 MCP，并把 MCP 工具追加到工具列表。
2. 取出上一轮保存的待观察窗口，把当前用户输入作为上一轮反馈证据。
3. 检索相关 Skills，并把 `<retrieved_skills>` 注入到用户消息中。
4. 根据配置走 OpenAI 或 Anthropic 兼容协议。
5. 模型返回文本或工具调用意图。
6. 工具调用进入统一调度入口，再分发到 Plan Mode、子 Agent、Skill、MCP 或内置工具。
7. 工具结果回写给模型，模型继续推理，直到不再调用工具。
8. 本轮结束后保存 assistant 输出，后台做 Skill 使用效果跟踪和在线自进化。
9. 设置新的待观察窗口，并自动保存 session。

### Q7：为什么说 `agents/agent.py` 是核心？

**回答：**  
因为 `agent.py` 负责把所有能力串成运行时闭环。它不仅调用模型，还维护两套协议消息历史、MCP 管理器、工具列表、权限模式、读取文件状态、Memory 注入状态、Skill 检索结果、待观察窗口、后台 Skill 任务和 session 信息。

更关键的是，`agent.py` 里的工具统一路由入口负责所有工具调度。无论是内置工具、Skill、MCP 还是子 Agent，都先进入这里，再由 Harness 决定怎么执行。

### Q8：项目里哪些地方体现了 Harness Agent 的设计？

**回答：**  
主要体现在三点：

第一，模型不直接执行动作，只返回工具调用意图。动作由 Harness 做权限检查和实际执行。

第二，工具结果不是简单打印，而是回写给模型，让模型基于真实环境反馈继续推理。

第三，任务结束后 Harness 继续负责状态管理，包括 session 保存、上下文压缩、Skill 使用效果跟踪和在线自进化。这说明项目的核心是 Runtime，而不是一次性模型调用。

## 三、模型协议与 Agent Loop

### Q9：OpenAI-compatible 和 Anthropic-compatible 是怎么兼容的？

**回答：**  
入口层会根据 API 配置判断协议。如果 base URL 路径包含 `/anthropic`，走 Anthropic-compatible；如果配置了 OpenAI base URL 或 OpenAI API key，走 OpenAI-compatible。

Agent 内部维护两套消息历史：一套给 OpenAI 兼容协议，一套给 Anthropic 兼容协议。OpenAI 路径按 OpenAI 的消息格式表达工具调用和工具结果；Anthropic 路径按 Anthropic 的消息块格式表达工具调用和工具结果。

但上层逻辑是共享的：Skills 检索、Memory 预取、MCP 初始化、权限判断、工具执行、session 保存和自进化都不关心底层协议。

### Q10：为什么不抽一个统一模型适配层？

**回答：**  
这是当前阶段的工程取舍。OpenAI 和 Anthropic 的消息结构、工具调用结构、系统提示词位置和工具结果格式差异比较明显。项目目标是保持轻量、可运行、可阅读，所以当前把差异限制在模型调用层，而不是过早抽象。

如果后续要支持更多模型，比如 Gemini、本地模型或多模态模型，我会抽出模型适配层，统一定义“流式调用模型”“解析工具调用”“格式化工具结果”这几类能力，把协议适配从 `Agent` 里进一步解耦。

### Q11：模型循环为什么不是只调用一次模型？

**回答：**  
Coding Agent 的任务通常需要多轮“观察环境”。模型第一次可能只知道要读文件，读完后才能决定怎么改；改完后还可能要运行测试。一次模型调用无法完成这种多步任务。

所以 Bear Code 的模型循环是：模型返回文本或工具调用；如果有工具调用，Runtime 执行工具并把结果回写；模型基于结果继续推理。直到模型不再调用工具，本轮对话才结束。

### Q12：项目里怎么做长上下文控制？

**回答：**  
有三类机制。

第一是手动压缩和自动压缩，当上下文接近窗口限制时，用模型总结历史对话。

第二是压缩流水线，会对工具结果做预算压缩、过期结果裁剪和微压缩，避免长工具输出持续占用上下文。

第三是大结果持久化，超过阈值的大工具结果会保存到 `~/.bear-code/tool-results/`，上下文里只保留预览和文件路径。

## 四、工具系统与权限控制

### Q13：工具系统是怎么设计的？

**回答：**  
工具定义集中在 `agents/tools.py` 的工具定义表里，每个工具有名称、描述和参数结构。模型看到的是这些结构，不是 Python 函数本身。

当前主要工具包括：

- 读文件、写文件、精确编辑
- 列文件、文本搜索
- Shell 命令执行
- Skill 调用、Skill 创建、Skill 演化
- 子 Agent
- 工具搜索
- 延后加载的计划模式进入和退出

执行时，模型发起工具调用，Agent 解析工具名和参数，先做权限检查，再路由到具体实现。

### Q14：什么是 deferred tools？为什么要做？

**回答：**  
延后加载工具是默认不直接暴露完整定义的工具。当前计划模式进入和退出工具属于这一类，模型默认只能从 system prompt 知道这些工具可以通过工具搜索获取。

这样做的目的有两个：第一，减少模型上下文里的工具定义噪音；第二，把低频或特殊工具按需激活。模型需要时先发起工具搜索，Runtime 返回匹配工具的完整定义，并把工具加入当前可用工具列表。

### Q15：权限模式有哪些？

**回答：**  
主要有五种：

| 模式 | 含义 |
|------|------|
| `default` | 敏感操作需要确认 |
| `acceptEdits` | 自动接受编辑类操作，适合自动沉淀 Skills |
| `bypassPermissions` | 尽量跳过确认，风险最高 |
| `dontAsk` | 自动拒绝需要确认的操作，适合 CI |
| `plan` | 只允许只读探索和写计划文件 |

权限入口是统一权限检查逻辑。它会结合当前模式、工具类型、settings 里的 permission rules、危险命令判断和用户确认函数，返回允许、拒绝或需要确认。

### Q16：怎么避免 Agent 乱改文件？

**回答：**  
主要有四层保护。

第一，权限模式控制。默认模式下新建文件、危险 shell、Skill 创建和演化都会确认。

第二，Plan Mode 控制。Plan Mode 下只能使用只读工具，并且只能写计划文件。

第三，编辑前读取保护。写入或编辑已有文件前，Runtime 要求模型先使用读文件工具，并记录文件修改时间；如果文件被外部修改，需要重新读取。

第四，精确编辑工具采用字符串替换，要求待替换内容唯一匹配，并返回差异，方便审计。

### Q17：为什么编辑前必须读取文件？

**回答：**  
这是为了避免模型基于过期或想象中的内容改文件。文件编辑型 Agent 的风险不是“会不会写”，而是“是否理解当前版本”。Bear Code 在读文件成功后记录文件绝对路径和修改时间；写入或编辑前检查这个状态。如果没读过或文件已变化，就拒绝操作并要求重新读取。

### Q18：Plan Mode 是怎么实现只读的？

**回答：**  
Plan Mode 有 prompt 约束和工具权限两层。

进入 Plan Mode 时，Agent 会生成一个 plan 文件路径，并把计划模式提示词拼到 system prompt，要求模型只读探索、设计方案、写计划，最后退出计划模式。

真正的硬约束在基础工具的权限检查逻辑。当 mode 是 `plan` 时，编辑工具会被拒绝，除非目标路径正好是当前 plan 文件；Shell 执行也会被拒绝。所以 Plan Mode 不是只靠 prompt，而是靠 Runtime 权限兜底。

## 五、Skills 体系

### Q19：Skills 是什么？

**回答：**  
Skills 是可复用任务方法的载体，落盘形式是 `SKILL.md`。它保存的不是某次任务内容，而是未来同类任务仍适用的方法、流程、风格或操作规范。

每个 Skill 有元数据，比如名称、描述、触发条件、执行上下文、允许使用的工具和是否允许用户手动调用，正文是具体指令。Runtime 会发现这些 Skills，写入 system prompt，也会按用户请求检索相关 Skills 并注入上下文。

### Q20：Skills 和 Memory 有什么区别？

**回答：**  
Memory 记事实和偏好，Skills 记方法和流程。

比如“这个项目部署在某台机器上”是 Memory；“以后写部署文档都按背景、步骤、验证、回滚风险组织”是 Skill。

简单说，Memory 解决“知道什么”，Skills 解决“怎么做”。

### Q21：Skill 是怎么加载的？

**回答：**  
Skill 发现逻辑会扫描两个目录：

```text
~/.bear/skills/<name>/SKILL.md
<cwd>/.bear/skills/<name>/SKILL.md
```

当前实现先加载用户级，再加载项目级；项目级加载时不会覆盖同名用户级 Skill，所以同名时用户级优先。每个 `SKILL.md` 会被解析成 `SkillDefinition`，后续用于 prompt 展示、检索和执行。

### Q22：Skill 检索怎么做？

**回答：**  
Skill 检索使用轻量 BM25 风格检索。它会把用户请求和 Skill 的名称、描述、触发条件、正文片段做词项匹配，其中元信息权重更高，正文也参与。中文会做相邻双字切分，英文做基础分词。

默认会返回最多 3 个命中，然后系统会生成 Skill 注入块，追加到当前用户消息里。模型看到这些候选后，再判断是否应该调用 Skill 工具。

### Q23：Skill 的 inline 和 fork 有什么区别？

**回答：**  
`inline` 是把 Skill prompt 返回给主 Agent，让主 Agent 在当前上下文里继续执行。适合轻量规则，比如代码审查格式、文档输出风格。

`fork` 是创建一个子 Agent，把 Skill prompt 作为子 Agent 的 system prompt，并按允许使用的工具限制工具范围。适合复杂任务，比如需要大量搜索、独立分析或隔离上下文的任务。

## 六、Skills 自进化

### Q24：自进化 Skills 是怎么实现的？

**回答：**  
核心是先观察、再沉淀。

当前轮用户任务结束后，系统不会马上把经验写成 Skill。它会先保存一个待观察窗口，里面记录这轮用户的问题、助手的回答，以及当时命中的相关 Skills。

等用户下一轮继续输入时，系统会把这句话当作对上一轮结果的反馈。比如用户说“以后都按这个格式来”“刚才那种写法不要再用”，这类反馈就会和上一轮记录合并起来，作为是否沉淀 Skill 的依据。

之后系统会判断这条反馈里有没有未来可复用的方法或规则。如果有，就整理成一个 Skill 候选，再和已有 Skills 做比较：如果是一个新的能力，就创建新的 `SKILL.md`；如果和已有 Skill 很接近，就合并到已有 Skill 里；如果只是一次性内容、价值不高或者证据不够，就不会写文件，只记录这次判断过程，方便后面追踪。

### Q25：为什么要用下一轮用户反馈，而不是当前轮结束就沉淀？

**回答：**  
因为当前轮结束时，Agent 不知道用户是否认可它的输出。如果立刻沉淀，容易把一次性任务内容、临时项目事实或 assistant 自己总结出的规则写成长期 Skill。

下一轮用户反馈通常更可靠，比如“以后都按这个格式来”“刚才那种写法不要再用了”“这类任务要先给结论”。这些是明确的稳定偏好，更适合作为 Skill 沉淀证据。

### Q26：为什么要把“找经验”和“写 Skill”拆开？

**回答：**  
拆开是为了降低“抽取即写入”的风险。

第一步只判断“这段对话里有没有可复用经验”，如果有，就整理成一个候选 Skill，但不会直接写文件。

第二步再判断“这个候选应该怎么放进现有 Skills 体系里”。如果已有 Skill 能承载，就合并进去；如果确实是新的能力，就新建；如果证据不足或价值不高，就放弃写入。

这样做可以避免系统一发现一句反馈就马上写文件，也能减少重复 Skill 和低质量 Skill。

### Q27：怎么避免沉淀垃圾 Skill？

**回答：**  
有几层控制。

第一，只沉淀用户明确表达的、未来同类任务仍适用的方法或偏好，不沉淀一次性任务内容、密钥、账号、URL、精确日期和临时项目事实。

第二，新候选会先和已有 Skills 做比较。如果已有 Skill 已经能覆盖这条经验，就优先合并，而不是随便新建。

第三，每次判断都会记录来源和结果。即使最后没有写 Skill，也能追踪当时为什么放弃。

第四，使用效果跟踪会统计“被检索、相关、已使用”。如果某个 Skill 长期被检索但没有实际使用，就可能被归档。

### Q28：Skill 落盘和演化怎么保证可审计？

**回答：**  
所有写入最终收敛到 `skill_evolution.py`。

新增 Skill 时，Skill 创建逻辑会生成目录和 `SKILL.md`，写入版本号、描述、触发条件和正文，并记录使用日志。

演化 Skill 时，Skill 演化逻辑会先保存演化前快照到历史目录，再更新补丁版本号、正文和元信息，然后写使用日志。

在线沉淀还会记录来源文件，里面包括这次判断对应的对话、命中的 Skill、最终选择和错误信息。这样后面可以回答三个问题：这个 Skill 从哪次反馈来，为什么新增或合并，修改前版本是什么。

## 七、在线 Skills 评测

### Q29：为什么还需要 Skills 评测？

**回答：**  
自进化不能只解决“能自动写 Skill”，还要解决“写出来的 Skill 是否真的有效”。否则能力库会越来越多，但质量不可控。

所以项目里设计了 `online_skill_eval.py`，对应 REPL 的 `/skill-eval`。它会基于真实在线记录构造回放样本，评估 Skill 是否被检索、是否相关、是否被使用、历史回复是否符合 Skill 规则，以及候选改进是否优于当前版本。

### Q30：`/skill-eval` 的整体流程是什么？

**回答：**  
流程是：

1. 读取 `.bear/skill-evolution/` 下的在线来源记录、Skill 使用统计和生命周期统计。
2. 读取当前正在使用的 Skills 快照。
3. 按 Skill 名聚合成 lineage。
4. 从历史对话记录构造回放样本池，并稳定切分成开发验证集和升级验证集。
5. 从 Skill 文本编译评测规则。
6. 对历史助手回复跑程序化规则和可选的模型辅助判断。
7. 结合回放样本、规则通过率、严重失败、被检索、相关、已使用等信号给 Skill 打状态。
8. 如果有辅助模型查询能力，就生成规则型候选版本和模型改写候选版本，重新生成回放回答，再用同一套规则比较。
9. 满足门控条件时记录健康版本产物。

### Q31：评测规则是怎么来的？

**回答：**  
评测规则编译逻辑会从 Skill 的名称、描述、触发条件、执行说明和标签中抽取规则。

默认一定会检查回复是否为空。如果 Skill 文本里提到引用来源，就增加“必须引用来源”规则；提到段落限制，就增加“段落数量限制”规则；提到先给结论，就增加“先给结论”规则；提到 JSON 或结构化输出，就增加“输出必须可解析”规则；提到表格，就增加“必须使用 Markdown 表格”规则；提到不要幻觉或不确定就说，会增加不编造相关规则。

如果 `/skill-eval` 是从 REPL 触发，并传入补充问题，还会启用模型辅助的二元判断，让模型判断历史回复是否符合 Skill 的可观察要求。

### Q32：Skill 状态有哪些？怎么判断？

**回答：**  
主要状态有：

| 状态 | 含义 |
|------|------|
| `unobserved` | 没有回放样本，也没使用信号 |
| `incubating` | 有信号但样本不足 |
| `watch` | 样本足够但规则或使用门槛不达标 |
| `healthy` | 回放规则和使用门槛都达标 |
| `pruned` | 已经因为长期无效被归档 |

默认阈值是回放样本至少 2 个，升级验证样本至少 1 个，被检索至少 5 次，使用率至少 20%，相关率至少 35%，规则通过率至少 80%。如果硬性规则在升级验证样本上失败，也会进入 watch。

### Q33：候选版本和健康版本记录是什么？

**回答：**  
候选版本是评测层根据失败原因生成的 Skill 改进版本，不会直接覆盖当前正在使用的 Skill。

有两类候选：

- 规则型候选：根据失败规则追加约束，比如要求引用来源、控制段落、输出 JSON。
- 模型改写候选：让模型基于失败规则小幅改写 Skill 的执行说明。

候选会在回放样本上重新生成回答，再用同一套规则评测，选出表现最好的候选。如果有升级验证样本，还会单独在这部分样本上验证。只有 Skill 本身状态是 `healthy`，并且候选分数超过当前健康版本、严重失败不更多，系统才会记录新的健康版本。

### Q34：当前评测方案有什么局限？

**回答：**  
有三个明显局限。

第一，当前 Skill 的评测主要使用历史 `latest_assistant`，不是完整重跑主 Agent，所以它评估的是历史线上表现，不是严格离线基准测试。

第二，程序化规则依赖关键词触发，复杂质量问题仍需要模型辅助判断。

第三，候选版本默认只写评测产物和健康版本记录，不会自动覆盖当前正在使用的 Skill。这是保守设计，避免模型因为少量回放失败就直接改线上能力，但也意味着真正升级当前 Skill 还需要人工或后续流程接管。

## 八、Memory、MCP 和子 Agent

### Q35：Memory 怎么设计？

**回答：**  
Memory 负责保存长期事实、用户偏好、项目背景和历史决策。它按当前项目路径 hash 隔离，保存在 `~/.BearCode/projects/<project_hash>/memory/`，避免不同项目之间互相污染。

运行时会先扫描 Memory headers，构造 manifest，再通过 side query 选择与当前用户请求相关的 Memory。Memory 预取是异步的，完成后会把相关内容注入到最后一条用户消息中。

### Q36：MCP 在项目里怎么接入？

**回答：**  
项目自己实现了 stdio JSON-RPC MCP Client，不依赖 MCP SDK。`McpManager` 会读取 `~/.bear/settings.json`、`<cwd>/.bear/settings.json` 和 `<cwd>/.mcp.json`，启动配置里的 MCP Server。

每个 Server 由 MCP 连接对象管理子进程，执行初始化、工具列表获取和工具调用。发现到的工具会包装成带服务名前缀的工具名，加入 Agent 的工具列表。模型调用时，工具统一调度入口会判断工具名前缀，再路由到对应 Server。

### Q37：为什么需要子 Agent？

**回答：**  
子 Agent 用来隔离上下文和任务范围。比如 `explore` 子 Agent 只读搜索代码，`plan` 子 Agent 只读输出方案，`general` 子 Agent 可以独立处理局部任务。这样主 Agent 不需要把所有探索细节都塞进自己的消息历史。

自定义子 Agent 通过 `~/.bear/agents/*.md` 和 `.bear/agents/*.md` 定义。和 Skills 不同，自定义 Agent 是项目级优先，同名项目级会覆盖用户级。

### Q38：Skill fork 和子 Agent 有什么关系？

**回答：**  
Skill fork 本质上复用了子 Agent 思路。Skill 的执行上下文如果是 `fork`，Runtime 会创建一个新的 Agent，把 Skill prompt 作为 system prompt，并根据允许使用的工具限制工具范围。执行完成后只把最终结果返回主 Agent。

这适合复杂 Skill：主 Agent 只需要拿到结果，不需要承载所有中间检索和推理过程。

## 九、工程难点与取舍

### Q39：项目最大的技术难点是什么？

**回答：**  
最大难点不是调用模型 API，而是把模型、工具、权限、上下文、长期记忆、自进化和评测串成稳定闭环。

具体难点有三类：

第一是执行安全。模型可以调用文件和 Shell，但所有副作用必须被 Runtime 控制。

第二是长期上下文。Memory 和 Skills 要分工清楚，否则事实、偏好、方法会混在一起。

第三是自进化治理。自动沉淀 Skill 很容易污染能力库，所以必须先保存待观察窗口，再用用户下一轮反馈作为证据，并记录来源、使用效果和评测结果。

### Q40：为什么不用 LangChain / AutoGen 这类框架？

**回答：**  
这个项目的目标是学习和实现 Coding Agent 底层机制，而不是快速拼应用。如果使用重型框架，很多关键细节会被封装掉，比如工具结果回写、权限判断、编辑前读取保护、Anthropic/OpenAI 工具调用差异、待观察窗口和 Skill 评测产物。

我选择自己实现 Harness，是为了把 Agent Runtime 的关键链路都掌握在项目里。这样更利于面试展示，也方便后续按自己的需求扩展。

### Q41：如果让你继续优化，你会做什么？

**回答：**  
我会优先做四件事。

第一，抽象模型适配层，把 OpenAI / Anthropic 的消息格式和工具格式适配进一步模块化。

第二，增强 Skills 检索，引入可选向量检索或 hybrid search，但保持 BM25-lite 作为无依赖 fallback。

第三，完善在线评测到当前 Skill 的升级流程。当前健康版本记录只是评测产物，不自动覆盖当前 Skill，后续可以加人工审批后的升级流程。

第四，补更多测试，尤其是权限模式、Plan Mode、Skill 新建/合并/放弃写入、MCP 工具路由和在线评测门控。

### Q42：这个项目有哪些风险或不足？

**回答：**  
主要有几类。

第一，当前项目还是轻量实现，模型协议适配没有抽象成独立 adapter。

第二，Skill 检索是轻量 BM25 风格，不适合大规模 Skill 库。

第三，在线评测依赖历史回放和模型辅助判断，不能完全替代人工质量评估。

第四，自进化虽然有权限和审计，但仍需要用户明确配置合适权限模式；在 `bypassPermissions` 下使用风险更高。

我会在面试中强调：项目的重点是“可运行、可审计、可扩展的 Harness 原型”，不是声称已经达到工业级 Agent 平台。

## 十、高频追问速答

### Q43：`skill_create` 和 `/skill-create` 有什么区别？

**回答：**  
Skill 创建工具是模型可调用的工具，走工具权限和基础工具执行器。Skill 创建命令是 REPL 命令，由用户手动输入，入口在 `main.py`，最终也会走同一套 Skill 落盘逻辑。两者最终落盘路径一致，但触发来源不同。

### Q44：为什么 Skill 创建和演化要确认？

**回答：**  
因为 Skill 会影响未来 Agent 行为，属于长期能力修改。错误 Skill 的影响范围比一次文件编辑更大，所以模型触发 Skill 创建或演化时，在默认权限下需要确认。

### Q45：MCP 工具和内置工具执行路径有什么不同？

**回答：**  
内置工具最终进入基础工具执行器；MCP 工具进入 MCP 管理器，再通过 JSON-RPC 调外部 Server。但它们都先经过工具统一调度入口，所以在 Harness 层是统一调度的。

### Q46：为什么工具结果要持久化？

**回答：**  
大工具结果如果直接放进上下文，会快速挤爆模型窗口。持久化后，模型只看到预览和路径；如果确实需要完整内容，可以再使用读文件工具。这是一种上下文预算管理。

### Q47：为什么用户级 Skill 优先于项目级 Skill？

**回答：**  
当前实现选择用户级优先，意味着用户个人长期偏好不会被某个项目同名 Skill 覆盖。这个选择适合“个人 Coding Agent”场景。如果转成团队项目，可能会改成项目级优先，避免团队规范被个人 Skill 覆盖。

### Q48：自定义 Agent 和 Skill 的覆盖规则一样吗？

**回答：**  
不一样。Skills 当前是用户级优先，同名项目级不覆盖用户级。自定义 Agent 是项目级优先，因为项目里的 agent 通常和当前代码库任务更强相关。

### Q49：为什么使用效果跟踪要判断相关和实际使用？

**回答：**  
只统计“被检索”不够。一个 Skill 被检索出来不代表它有用。系统还要判断它是否和任务相关，以及最终回答是否真的体现了这个 Skill。这样才能发现“经常被召回但不被使用”的低效 Skill。

### Q50：这个项目怎么体现可审计？

**回答：**  
可审计体现在多个层面：

- 文件编辑返回 diff。
- 会话自动保存，可 resume。
- Skill 新增和演化有 usage log。
- 演化前有 history 快照。
- 在线沉淀有来源记录。
- Skill 使用有“被检索、相关、已使用”统计。
- 在线评测有回放样本池、评测规则、输出记录、判断记录、总结和健康版本产物。

## 十一、面试收尾表达

### Q51：你觉得这个项目最能体现你能力的地方是什么？

**回答：**  
我觉得最能体现能力的是系统设计和工程边界。这个项目不是堆功能，而是围绕 Agentic Harness 建了一套闭环：模型协议适配、工具调用、权限控制、上下文管理、长期记忆、Skills、自进化、MCP、子 Agent 和在线评测都在一个 Runtime 里协作。

尤其是 Skills 自进化部分，我没有让模型直接改 prompt，而是先保存待观察窗口，等下一轮用户反馈进来后再判断是否沉淀，并且记录来源、版本、使用效果和在线评测结果。这样“自动沉淀能力”不是黑箱行为，而是一个可治理的流程。这比单纯做一个能聊天或能改文件的 Agent 更有工程挑战。

### Q52：如果面试官只给 30 秒，你怎么讲？

**回答：**  
Bear Code 是我用 Python 从零实现的一个自进化命令行 Coding Agent。它的核心是 Agentic Harness：统一编排 OpenAI/Anthropic 兼容模型、工具调用、权限控制、上下文压缩、Memory、Skills、MCP 和子 Agent。最大亮点是 Skills 自进化和评测：系统会把用户下一轮反馈作为证据沉淀成 `SKILL.md`，再通过历史回放、规则编译、模型辅助判断、候选版本试跑和健康版本记录评估 Skill 是否稳定可用。

### Q53：如果面试官只给 2 分钟，你怎么讲？

**回答：**  
Bear Code 是我实现的本地 Coding Agent Runtime，底层是一套自研 Agentic Harness。用户请求进入主对话流程后，系统会检索 Skills、预取 Memory、初始化 MCP，然后根据配置选择 OpenAI-compatible 或 Anthropic-compatible 模型。模型如果返回工具调用意图，Harness 会统一做权限检查，再分发到内置工具、Skill、MCP 或子 Agent，执行结果回写给模型形成多轮闭环。

项目的第二条主线是长期能力沉淀。Memory 保存事实和偏好，Skills 保存可复用方法。每轮任务结束后，系统会先保存一个待观察窗口；下一轮用户反馈进来后，再判断里面有没有未来可复用的方法。如果有，就和已有 Skills 比较，决定新建 `SKILL.md`、合并到已有 Skill，或者只记录判断过程不写文件。整个过程会记录来源、历史版本和使用效果。

最新的一层是在线评测。`/skill-eval` 会从来源记录构造回放样本池，从 Skill 文本编译程序规则和模型辅助判断规则，结合“是否被检索、是否相关、是否真的用上”给 Skill 打 `unobserved`、`incubating`、`watch`、`healthy` 等状态；如果有失败，还会生成候选版本试跑，并通过健康版本门控记录表现更好的版本。整体目标是让 Agent 不只是能执行任务，还能把稳定经验沉淀成可审计、可比较、可持续演化的能力。
