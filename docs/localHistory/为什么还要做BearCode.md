# 为什么还要做 Bear Code

Claude Code、Codex 这类 Coding Agent 已经很强，能理解需求、搜索代码、修改文件、运行命令和完成复杂工程任务。Bear Code 不是为了再做一个同类命令行聊天工具，也不是为了证明“我也能调模型 API”。它要回答的是另一个问题：

**当模型能力已经足够强之后，我们还需要什么样的工程系统，才能把 Agent 的执行过程做得可控、可观察、可沉淀、可评测，并且更适合长程任务？**

Bear Code 的价值不在于替代成熟产品，而在于把 Coding Agent 背后的关键运行时能力拆开、实现出来，并形成一个可以阅读、修改、验证和继续演进的自研 Agentic Harness。

## 1. 现成工具强，但内部运行时不可控

Claude Code / Codex 更像成熟产品，优势是开箱即用、模型能力强、工具链完整。但如果想研究或改造 Agent 底层机制，现成产品通常不会完整暴露内部链路，例如：

- 模型如何决定调用哪个工具；
- tool call 如何经过权限检查；
- 上下文变长后如何压缩；
- 长程任务中如何保留任务状态；
- 用户反馈如何沉淀成长期能力；
- 沉淀出来的能力如何评测和审计；
- 子 Agent、MCP、Skills 如何进入同一个运行时。

Bear Code 做的是这一层：把模型调用、工具系统、权限控制、会话折叠、长期上下文、Skills 自进化、MCP 和子 Agent 组织成一个可运行的 Runtime。它不是黑盒产品，而是一个可以继续实验和扩展的底座。

## 2. Bear Code 的核心不是聊天，而是 Harness

普通模型壳子通常是：

```text
用户输入 -> 调用模型 -> 输出回复
```

Bear Code 的核心链路是：

```text
用户输入
  -> 构建 system prompt、Memory、Skills 和 MCP 工具上下文
  -> 调用 OpenAI / Anthropic 兼容模型
  -> 解析 tool call
  -> 权限检查
  -> 执行内置工具 / Skills / MCP 工具 / 子 Agent
  -> 工具结果回写模型
  -> 必要时折叠会话记忆
  -> 保存 session
  -> 后台触发 Skills 自进化和评测审计
```

这里的关键差异是：模型只负责提出意图，真正的本地环境动作由 Runtime 管理。读文件、写文件、执行 Shell、调用 MCP、启动子 Agent、创建或演化 Skill，都要经过统一的工具分发和权限边界。

这也是为什么 Bear Code 可以被称为 Agentic Harness：它不是单点功能，而是一套把模型能力接入真实工程环境的运行时控制层。

## 3. 为什么要做自主会话记忆折叠

长程任务中，Agent 最大的问题之一不是“不会回答”，而是上下文会不断膨胀：

- 文件读取结果越来越多；
- 工具失败和重试路径越来越多；
- 中间推理和临时结论越来越多；
- 当前目标、已完成步骤、下一步动作容易被淹没。

简单截断上下文会丢失关键信息；普通摘要又容易把任务状态压成一段不可操作的自然语言。Bear Code 借鉴 DeepAgent 的 memory folding 思路，把会话折叠成三层结构化状态：

```text
episode_memory
  -> 任务描述、关键事件、当前进度

working_memory
  -> 当前子目标、阻塞点、下一步动作

tool_memory
  -> 已用工具、有效参数、常见错误、返回模式、工具经验
```

这个机制的目的不是单纯省 token，而是让 Agent 在长程任务中完成一次状态重组：

- 保留任务进展，避免折叠后断片；
- 保留当前目标和阻塞点，继续推进任务；
- 保留工具经验，减少重复失败；
- 在策略变化或工具反复失败时，让 Agent 有机会重新整理局面。

当前代码中，模型可以主动调用 `compact_context`，系统也会在上下文接近阈值时自动触发折叠。折叠后的结构化状态会写入当前项目 `.bear/sessions/`，并回注到当前会话中继续执行。

这就是 Bear Code 面向长程任务的关键设计：不是只把历史变短，而是把历史重组为可继续执行的 session state。

## 4. 为什么要做 Skills 自进化

很多用户反馈不是一次性的，例如：

- “简历内容不要写成 STAR 标题，要能直接贴到简历里。”
- “测评文档只保留 Pass@1，其他指标删掉。”
- “这类回答先给结论，再解释理由。”
- “不要沉淀临时项目事实，只沉淀可复用方法。”

如果这些反馈只留在当前上下文里，下次相似任务仍然需要用户重复说明。Bear Code 的 Skills 自进化就是把这类稳定反馈沉淀成 `SKILL.md`。

它沉淀的不是某次任务内容，而是可复用能力：

- 输出规范；
- 工作流；
- 实现偏好；
- 任务触发条件；
- 可复用的判断规则。

自进化链路不是让模型随便改 prompt，而是拆成明确流程：

```text
本轮用户请求 + assistant 回复
  -> 保存 pending window
下一轮用户反馈
  -> 合并为证据
  -> 抽取器提取最多一个候选 Skill
  -> 维护器检索相似 Skills
  -> 决策 add / merge / discard
  -> 写入或演化 SKILL.md
  -> 记录来源追踪、版本快照和使用统计
```

这个设计的重点是：用用户后续反馈作为证据，而不是让模型自我总结“我学到了什么”。这样能降低幻觉式沉淀，也能避免把一次性内容、隐私、URL、账号 ID、精确日期和临时项目事实写进长期能力。

## 5. 为什么还要做 Skills 评测

自动沉淀能力之后，还需要回答一个更现实的问题：这个 Skill 真的可靠吗？

Bear Code 没有把新建或演化后的 Skill 默认当成健康版本，而是设计了在线 Skills 评测链路：

```text
读取 online provenance / usage stats / active skills
  -> 按 Skill 聚合 lineage
  -> 从历史对话构造 replay 样本池
  -> 从 SKILL.md 编译规则
  -> 评估历史 assistant 回复
  -> 可选启用 LLM judge
  -> 根据失败规则生成候选版本
  -> 重新生成 replay 回复并评测
  -> 满足 gate 时记录 champion
```

这条链路让自进化变得可回放、可比较、可审计：

- replay 说明“用哪些历史样本评测”；
- 规则评测说明“为什么通过或失败”；
- LLM judge 用于检查更软性的指令对齐；
- 候选版本试跑用于比较改进是否有效；
- champion 记录当前本地已知的健康版本。

需要注意的是，当前 champion 是评测产物，不会自动覆盖 active Skill。这是有意保守的设计：能力演化应该可观察、可回滚，而不是让模型在后台无约束修改自己的线上能力。

## 6. 为什么要自己实现工具、权限、MCP 和子 Agent

Bear Code 不是只关注模型输出，还关注模型如何安全地连接本地环境。

### 工具与权限

模型可以提出 `read_file`、`edit_file`、`run_shell`、`skill_create` 等动作，但是否允许执行由 Runtime 决定。当前实现包括：

- Plan Mode 下阻断普通写操作和 Shell；
- 危险 Shell、新建文件、Skill 创建和演化需要确认；
- 编辑已有文件前必须先读取文件；
- 记录 mtime，避免基于过期文件内容覆盖用户修改。

这部分的意义是把“模型意图”和“环境动作”隔开。模型不是本地环境的直接操作者，Harness 才是动作边界。

### MCP

MCP 让外部工具可以通过统一协议接入。Bear Code 自己实现了 stdio JSON-RPC MCP Client，完成配置读取、server 启动、initialize、tools/list 和 tools/call 路由。外部工具最终会被包装成 `mcp__server__tool` 形式交给模型调用。

这让 Bear Code 不需要把所有工具硬编码进项目，而是保留了扩展外部能力的接口。

### 子 Agent

子 Agent 用于隔离上下文和任务范围。比如：

- `explore` 用只读工具快速搜索代码；
- `plan` 用只读工具生成计划；
- `general` 独立处理局部任务；
- 自定义 Agent 可以通过 `.bear/agents/*.md` 扩展。

这对长程任务很重要：主 Agent 不必把所有探索细节都塞进自己的上下文里，可以把局部任务交给隔离上下文执行。

## 7. 为什么它适合做长程任务

Bear Code 的长程任务能力来自几个机制的组合，而不是单个功能：

- 自主会话记忆折叠让长历史变成结构化 session state；
- Memory 记项目事实和用户偏好；
- Skills 记可复用任务流程；
- 子 Agent 隔离局部探索；
- MCP 扩展外部工具；
- session 保存支持任务恢复；
- 工具结果截断和大结果持久化降低上下文噪声；
- Skills 自进化让稳定反馈跨会话保留。

也就是说，Bear Code 不是只让模型“想得更久”，而是给长程任务提供状态管理、工具经验、能力沉淀和恢复机制。

当前 `wiki/测评.md` 中也用 GAIA / HLE 做了长程和高难度任务评估：

| 数据集 | 题数 | Pass@1 |
| --- | ---: | ---: |
| GAIA | 165 | 53.3 |
| HLE | 500 | 20.2 |

其中 GAIA 记忆折叠消融显示：开启结构化会话记忆折叠时 Pass@1 为 53.3，关闭后降至 44.7，下降 8.6 个百分点。这说明会话折叠的收益不只是减少上下文长度，而是帮助 Agent 在长程任务中保留状态并继续推进。

## 8. 为什么轻量化仍然重要

Bear Code 没有追求把所有功能做成重型平台，而是保留一个足够清楚、可读、可改的核心面。

这种轻量化有几个好处：

- 源码路径短，便于理解完整 Agent Loop；
- 功能边界清楚，方便替换模型、工具、检索和存储方案；
- Skills 直接落成文件系统里的 `SKILL.md`；
- 审计信息写入本地 `.bear/skill-evolution/`；
- 评测产物可以直接查看和回放；
- 适合继续做实验，而不是被复杂平台结构锁死。

对学习、研究和二次开发来说，轻量不是功能弱，而是修改成本低、验证路径短。

## 9. Bear Code 的真正定位

如果用一句话概括，Bear Code 不是 Claude Code / Codex 的替代品，而是一个用于研究和实现 Coding Agent 底层机制的自研 Runtime。

它补的是成熟产品通常不会完整开放的那一层：

- 自己的工具权限边界；
- 自己的会话记忆折叠；
- 自己的 Skills 沉淀规则；
- 自己的自进化审计链路；
- 自己的 replay 评测和 champion 记录；
- 自己的 MCP 和子 Agent 接入方式。

这让 Bear Code 既可以作为理解 Claude Code 类工具底层机制的样本，也可以作为个人或团队继续改造的 Agentic Harness 底座。

## 10. 一句话总结

Bear Code 之所以值得做，不是因为现成 Coding Agent 不够强，而是因为我们需要一个**足够轻、足够清楚、足够可控、足够可演化、并且可以评测的 Agentic Harness**。

它真正验证的是：

**当模型能力已经到位之后，工程系统如何把一次性对话转化为可持续推进的任务状态，把用户反馈转化为可复用 Skills，并用可回放、可比较、可审计的方式让 Agent 能力持续演进。**
